<h1>TOTALS</h1>
<div class = 'order-receipt'>
<h3>Total Service Cost: </h3>
  <%= @order.service.name %>: <%= number_to_currency(@order.service.hourly_rate) %>/hr
</div>

<div class = 'extra-receipt'>
<h3>Total Snack/Drink Cost: </h3>
  <% @order.order_extras.map do |extra| %>
    <%= Extra.find(extra.extra_id).name %> (<%= extra.quantity %>) 
      <ul>
      <li>Price: <%= number_to_currency(extra.price) %></li>
      <li>Subtotal: <%= number_to_currency(extra.price * extra.quantity) %></li>
      </ul>
  <% end %>
</div>

<h3>Total previous Charges: </h3>
<h2>Total Bill: </h2>

<%= button_to "Checkout" %>